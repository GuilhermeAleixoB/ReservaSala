<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.reservasala">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".lista.salas" />
        <activity android:name=".cadastro.cadastroSala" />
        <activity android:name=".delete" />
        <activity android:name=".lista.reservas" />
        <activity android:name=".cadastro.reserva" />
        <activity android:name=".lista.equipSalas" />
        <activity android:name=".lista.equipamentos" />
        <activity android:name=".cadastro.cadastroEquipamento" />
        <activity android:name=".cadastro.equipamentoSala" />
        <activity android:name=".cadastro.cadastroLogin" />
        <activity android:name=".MainActivity" />
        <activity android:name=".login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>